# API URL Configuration (Shared by Mobile, PWA, and Desktop)
# For local development on Android emulator, use 10.0.2.2 (emulator's host machine)
# Desktop will automatically replace 10.0.2.2 with localhost
EXPO_PUBLIC_API_URL=http://10.0.2.2:8080

# Desktop Specific (Optional - only if you need different URL than mobile)
# VITE_API_URL=http://localhost:8080

# Google OAuth Client ID (Desktop only)
VITE_GOOGLE_CLIENT_ID=your_google_client_id
